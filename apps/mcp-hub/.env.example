# MCP Hub Configuration

# Mode: "public" (download-only) or "local" (with deployment)
# Default: "public"
MODE=public

# Cloudflare Deployment (Phase 4)
# Set to "true" to enable Cloudflare deployment
# Default: false (deployment disabled for security)
ENABLE_CLOUDFLARE_DEPLOY=false

# Cloudflare Credentials (only needed if ENABLE_CLOUDFLARE_DEPLOY=true)
# These are read by wrangler from environment or wrangler.toml
# Do NOT commit these values to version control
CF_ACCOUNT_ID=
CF_API_TOKEN=

# Server Port
# Default: 4000
PORT=4000
