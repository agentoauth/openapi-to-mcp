{
  "tools": [
    {{#each tools}}
    {
      "name": "{{name}}",
      "description": "{{description}}",
      "input_schema": {{{json inputSchema}}},
      "output_schema": {{{json outputSchema}}}
    }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
}



