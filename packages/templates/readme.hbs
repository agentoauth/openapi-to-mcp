# {{serviceName}} MCP

Generated by [`openmcp`](https://www.npmjs.com/package/openmcp).

## Setup

```bash
npm install
npm run build
```

## Usage

The server listens on stdin/stdout using a simple JSON-RPC 2.0 tools/execute method.

### Running the server

```bash
node dist/index.js
```

{{#if authConfig}}
### Authentication

This MCP server requires authentication. Set the following environment variable:

```bash
{{authConfig.envVar}}="your-token-here" node dist/index.js
```

Or use a `.env` file (see `.env.example` for reference):

```bash
# Copy the example file
cp .env.example .env

# Edit .env with your actual token
# Then run:
export $(cat .env | xargs) && node dist/index.js
```
{{/if}}

{{#if apiBaseUrl}}
### API Base URL

The API base URL is configured as: `{{apiBaseUrl}}`

To override it, set the `API_BASE_URL` environment variable:

```bash
API_BASE_URL="https://your-api-base-url.com" node dist/index.js
```
{{/if}}

## Connecting to MCP Clients

You can connect this MCP to an MCP-compatible client by configuring the command:

```
node dist/index.js
```

Make sure to set any required environment variables (see Authentication section above) before starting the client.



